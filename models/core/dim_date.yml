version: 2

models:
  - name: dim_date
    columns:
      - name: date_day
        data_type: date
        data_tests:
          - unique
          - not_null
      - name: month
        data_type: int64
      - name: month_name
        data_type: date
      - name: year
        data_type: int64
